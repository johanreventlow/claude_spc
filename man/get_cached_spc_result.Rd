% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_spc_cache.R
\name{get_cached_spc_result}
\alias{get_cached_spc_result}
\title{Get Cached SPC Result}
\usage{
get_cached_spc_result(cache_key, cache)
}
\arguments{
\item{cache_key}{character. Cache key from \code{generate_spc_cache_key()}.}

\item{cache}{cache object. QIC cache instance from \code{get_or_init_qic_cache()}.}
}
\value{
Cached SPC result (list with plot, qic_data, metadata) or NULL if
cache miss or expired.
}
\description{
Retrieves cached SPC computation result if available and not expired.
}
\examples{
\dontrun{
cache_key <- generate_spc_cache_key(data, config)
qic_cache <- get_or_init_qic_cache(app_state)
cached_result <- get_cached_spc_result(cache_key, qic_cache)

if (!is.null(cached_result)) {
  # Cache hit - use cached result
  print(cached_result$plot)
} else {
  # Cache miss - compute fresh
  result <- compute_spc_results_bfh(...)
}
}

}
