% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_event_context_handlers.R
\name{utils_event_context_handlers}
\alias{utils_event_context_handlers}
\title{Event Context Handlers (Strategy Pattern)}
\description{
Context-aware event handling using strategy pattern to reduce cyclomatic
complexity and improve maintainability.
}
\details{
\subsection{Architecture}{

Instead of complex if/else chains based on context strings, this module
implements the \strong{Strategy Pattern} where each context type has its own
handler function.

\strong{Benefits}:
\itemize{
\item Reduced cyclomatic complexity (12 → ~3 per function)
\item Easier to add new context types (just add handler)
\item Better testability (test each handler independently)
\item Self-documenting code (function names describe behavior)
}
}

\subsection{Usage}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# In event listeners:
observeEvent(app_state$events$data_updated, \{
  update_context <- app_state$last_data_update_context
  handle_data_update_by_context(
    update_context,
    app_state,
    emit,
    input,
    output,
    session,
    ui_service
  )
\})
}\if{html}{\out{</div>}}
}
}
