% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_validation_guards.R
\name{validate_data_or_return}
\alias{validate_data_or_return}
\title{Validate Data or Return Fallback}
\usage{
validate_data_or_return(data, fallback = NULL, min_rows = 1, min_cols = 1)
}
\arguments{
\item{data}{Data object to validate}

\item{fallback}{Value to return if validation fails (default: NULL)}

\item{min_rows}{Minimum required rows (default: 1)}

\item{min_cols}{Minimum required columns (default: 1)}
}
\value{
Data if valid, otherwise fallback
}
\description{
Konsolideret guard for data validation med fallback.
Erstatter mønstret: if (!is.null(data) && nrow(data) > 0) { ... }
}
\examples{
# Basic usage
validated_data <- validate_data_or_return(input_data, fallback = data.frame())

# Custom thresholds
validated_data <- validate_data_or_return(input_data, min_rows = 10, min_cols = 3)
}
