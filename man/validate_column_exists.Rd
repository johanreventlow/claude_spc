% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_validation_guards.R
\name{validate_column_exists}
\alias{validate_column_exists}
\title{Validate Column Exists}
\usage{
validate_column_exists(
  data,
  column_name,
  return_column = FALSE,
  fallback = NULL
)
}
\arguments{
\item{data}{Data frame to check}

\item{column_name}{Column name to validate}

\item{return_column}{If TRUE, return column data; if FALSE, return logical (default: FALSE)}

\item{fallback}{Value to return if validation fails (default: NULL for column data, FALSE for logical)}
}
\value{
Column data, logical, or fallback depending on parameters
}
\description{
Konsolideret guard for kolonne validering.
Erstatter mønstret: if (!is.null(col) && col \%in\% names(data)) { ... }
}
\examples{
# Check if column exists
has_column <- validate_column_exists(data, "Dato")

# Get column data if exists
column_data <- validate_column_exists(data, "Dato", return_column = TRUE, fallback = numeric(0))
}
