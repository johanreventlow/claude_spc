% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_config_objects.R
\name{generateSPCPlot_v2}
\alias{generateSPCPlot_v2}
\title{Generate SPC Plot (New Parameter Objects API)}
\usage{
generateSPCPlot_v2(data, config, plot_cfg, viewport, phases)
}
\arguments{
\item{data}{Data frame with SPC data}

\item{config}{Configuration object}

\item{plot_cfg}{SPC plot configuration object (from \code{spc_plot_config()})}

\item{viewport}{Viewport dimensions object (from \code{viewport_dims()})}

\item{phases}{Phase configuration object (from \code{phase_config()})}
}
\value{
List with plot and qic_data
}
\description{
Generates an SPC plot using parameter objects instead of individual parameters.
This is the new preferred API that reduces function signature complexity.
}
\details{
This function provides a cleaner API with logical parameter grouping:
\itemize{
\item \code{plot_cfg}: All plot-specific configuration
\item \code{viewport}: Physical dimensions and scaling
\item \code{phases}: Phase/shift handling
}

\strong{Migration Example}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Old API (15 parameters):
plot <- generateSPCPlot(data, config, "p", target_value = 50, ...)

# New API (3 parameter objects):
plot_cfg <- spc_plot_config(chart_type = "p", target_value = 50)
viewport <- viewport_dims(width = 800, height = 600)
phases <- phase_config(show_phases = FALSE)
plot <- generateSPCPlot_v2(data, config, plot_cfg, viewport, phases)
}\if{html}{\out{</div>}}
}
