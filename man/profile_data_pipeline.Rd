% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_profiling.R
\name{profile_data_pipeline}
\alias{profile_data_pipeline}
\title{Profile Data Processing Pipeline}
\usage{
profile_data_pipeline(pipeline_steps, input_data)
}
\arguments{
\item{pipeline_steps}{Named list af processing functions}

\item{input_data}{Data at processe gennem pipeline}
}
\value{
Data.frame med timing for hvert trin
}
\description{
Specialiseret profiling for data processing workflows.
Måler performance på hvert trin i pipeline.
}
\examples{
\dontrun{
# Profile en data processing pipeline
pipeline_profile <- profile_data_pipeline(
  pipeline_steps = list(
    load = function(d) readr::read_csv(d),
    clean = function(d) d \%>\% filter(!is.na(value)),
    transform = function(d) d \%>\% mutate(scaled = scale(value)),
    aggregate = function(d) {
      d \%>\%
        group_by(category) \%>\%
        summarise(mean = mean(scaled))
    }
  ),
  input_data = "data.csv"
)

print(pipeline_profile)
}

}
\keyword{internal}
