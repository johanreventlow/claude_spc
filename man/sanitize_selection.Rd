% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui_utils_ui_helpers.R
\name{sanitize_selection}
\alias{sanitize_selection}
\title{Robust input sanitization for character(0) and NA handling}
\usage{
sanitize_selection(input_value)
}
\arguments{
\item{input_value}{Input value to sanitize (can be any type)}
}
\value{
Sanitized value or NULL if invalid/empty
}
\description{
Centralized input sanitization function that handles character(0),
NA values, empty strings, and vectors. Used throughout the app
for consistent dropdown and input validation.
}
\details{
Handles the following cases:
\itemize{
\item NULL values → NULL
\item character(0) → NULL
\item Vectors with all NA → NULL
\item Vectors longer than 1 → first element only
\item Single NA values → NULL
\item Empty or whitespace-only strings → NULL
\item Valid values → unchanged
}
}
\examples{
sanitize_selection(NULL)           # → NULL
sanitize_selection(character(0))   # → NULL
sanitize_selection(c(NA, NA))      # → NULL
sanitize_selection(c("a", "b"))    # → "a"
sanitize_selection("")             # → NULL
sanitize_selection("  ")           # → NULL
sanitize_selection("valid")        # → "valid"

}
\concept{input_validation}
