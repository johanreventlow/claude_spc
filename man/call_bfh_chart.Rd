% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_spc_bfh_service.R
\name{call_bfh_chart}
\alias{call_bfh_chart}
\title{Safely Invoke BFHchart with Error Handling}
\usage{
call_bfh_chart(bfh_params)
}
\arguments{
\item{bfh_params}{list. Named list of BFHchart-compatible parameters from
\code{map_to_bfh_params()}. Typically includes: data, x, y, chart_type, freeze, part.}
}
\value{
BFHchart result object (ggplot2 object). Returns NULL on error
(with structured error logging).
}
\description{
Wraps BFHchart function call with \code{safe_operation()} to provide graceful
error handling and structured logging. Prevents crashes from BFHchart API
errors and enables detailed error diagnostics.
}
\details{
\strong{Error Handling Strategy:}
\itemize{
\item Wrap BFHchart call in \code{safe_operation()} with informative operation name
\item Log BFHchart invocation parameters (sanitized for log safety)
\item Capture and structure BFHchart errors with full context
\item Return NULL on failure to enable fallback logic upstream
\item Performance logging (execution time per call)
}

\strong{Logging Context:}
Component tag: \verb{[BFH_SERVICE]}
Logged data: chart type, data dimensions, parameter summary, execution time
}
\examples{
\dontrun{
# Basic invocation
bfh_params <- map_to_bfh_params(...)
bfh_result <- call_bfh_chart(bfh_params)

if (is.null(bfh_result)) {
  log_error("BFHchart call failed", .context = "SPC_RENDERER")
  return(NULL)
}

# With timing
start_time <- Sys.time()
bfh_result <- call_bfh_chart(bfh_params)
elapsed <- difftime(Sys.time(), start_time, units = "secs")
log_info(paste("BFHchart call completed in", elapsed, "seconds"))
}

}
\seealso{
\code{\link{compute_spc_results_bfh}} for facade interface
\code{\link{map_to_bfh_params}} for parameter preparation
}
