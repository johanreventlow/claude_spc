% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_plot_diff.R
\name{detect_plot_update_type}
\alias{detect_plot_update_type}
\title{Detect Plot Update Type}
\usage{
detect_plot_update_type(current_state, previous_state)
}
\arguments{
\item{current_state}{List with current plot state (data, metadata, config)}

\item{previous_state}{List with previous plot state (data, metadata, config)}
}
\value{
List with update_type and changed_fields
}
\description{
Analyzes changes between current and previous plot state to determine
the minimum update needed (none, metadata only, or full regeneration).
}
\details{
Update types:
\itemize{
\item "none": No changes detected
\item "metadata_only": Only metadata changed (title, target, etc.)
\item "data_changed": Data or config changed, full regeneration needed
}
}
\examples{
current <- list(
  data = data.frame(x = 1:10, y = rnorm(10)),
  metadata = list(title = "New Title", target = 10),
  config = list(x_col = "x", y_col = "y", chart_type = "run")
)

previous <- current
previous$metadata$title <- "Old Title"

diff <- detect_plot_update_type(current, previous)
diff$update_type # "metadata_only"
diff$changed_fields # c("title")
}
