% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_plot_diff.R
\name{apply_metadata_update}
\alias{apply_metadata_update}
\title{Apply Metadata-Only Update to Plot}
\usage{
apply_metadata_update(plot, metadata, changed_fields)
}
\arguments{
\item{plot}{Existing ggplot2 plot object}

\item{metadata}{New metadata to apply}

\item{changed_fields}{Character vector of changed field names}
}
\value{
Updated plot object
}
\description{
Efficiently updates plot with changed metadata without full regeneration.
Uses ggplot2 layer modification for performance.
}
\details{
This function modifies plot layers in-place for metadata changes:
\itemize{
\item title: Updates plot title via labs()
\item target_value: Updates geom_hline for target
\item centerline_value: Updates geom_hline for centerline
\item y_axis_unit: Updates y-axis label
}

Much faster than full plot regeneration (~10-50ms vs ~200-500ms).
}
\examples{
# Assume we have an existing plot
library(ggplot2)
plot <- ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point()

# Update only title
new_meta <- list(title = "Updated Title")
plot <- apply_metadata_update(plot, new_meta, changed_fields = "title")
}
