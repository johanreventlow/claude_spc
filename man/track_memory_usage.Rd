% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_profiling.R
\name{track_memory_usage}
\alias{track_memory_usage}
\title{Track Memory Usage Over Time}
\usage{
track_memory_usage(session, interval_seconds = 60, max_samples = 100)
}
\arguments{
\item{session}{Shiny session object}

\item{interval_seconds}{Sampling interval (default: 60)}

\item{max_samples}{Maximum samples at gemme (default: 100)}
}
\value{
ReactiveVal med memory log data.frame
}
\description{
Periodic memory usage tracking for leak detection og performance monitoring.
Samples memory every interval_seconds og gemmer i reactive log.
}
\details{
Memory tracking registrerer:
\itemize{
\item Timestamp for hver sample
\item Total memory usage i MB
\item Memory change siden sidste sample
\item Session activity status
}

Stops automatisk når session ender.
}
\examples{
\dontrun{
# Start memory tracking i server function
memory_log <- track_memory_usage(
  session = session,
  interval_seconds = 30
)

# Check memory trends
observeEvent(input$show_memory, {
  current_log <- memory_log()
  print(tail(current_log, 10))

  # Check for memory leaks
  if (nrow(current_log) > 5) {
    recent_trend <- lm(
      memory_mb ~ timestamp,
      data = tail(current_log, 5)
    )
    if (coef(recent_trend)[2] > 5) {
      warning("Potential memory leak detected")
    }
  }
})
}

}
