% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_export_png.R
\name{generate_png_export}
\alias{generate_png_export}
\title{Generate PNG Export}
\usage{
generate_png_export(
  plot_object,
  width_inches = 10,
  height_inches = 7.5,
  dpi = 96,
  output_path = NULL
)
}
\arguments{
\item{plot_object}{ggplot object (fra generateSPCPlot eller applyHospitalTheme)}

\item{width_inches}{Numeric width i inches (e.g., 10 for standard, 7.5 for PowerPoint)}

\item{height_inches}{Numeric height i inches (e.g., 7.5 for standard, 5.625 for PowerPoint)}

\item{dpi}{Numeric DPI resolution (72, 96, 150, 300)}

\item{output_path}{Character path for output file (NULL = create tempfile)}
}
\value{
Character path til genereret PNG fil, eller NULL ved fejl
}
\description{
Genererer PNG export af en ggplot2 graf med specificerede dimensioner og DPI.
Understøtter både pixels og inches som input units, og sikrer exact pixel
output med ±1 pixel tolerance.
}
\details{
PNG generation workflow:
\enumerate{
\item Validate plot object (must be ggplot)
\item Create temporary file hvis output_path er NULL
\item Use ggplot2::ggsave() med device = "png" for high-quality rendering
\item Set bg = "white" for consistent background
\item Return file path eller NULL ved fejl
}

Dimension calculation:
\itemize{
\item Final pixel dimensions = width_inches × dpi × height_inches × dpi
\item Example: 10 inches × 96 DPI = 960 pixels width
\item Tolerance: ±1 pixel due to rounding
}

Error handling:
\itemize{
\item NULL plot → return NULL med log error
\item Invalid plot (not ggplot) → return NULL med log error
\item File write failure → return NULL med log error
\item All errors logged via log_error()
}
}
\examples{
\dontrun{
# Basic usage
plot <- ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point()
generate_png_export(plot, width_inches = 10, height_inches = 7.5, dpi = 96)

# With custom output path
generate_png_export(plot, 8, 6, 150, "output.png")

# High resolution for print
generate_png_export(plot, 10, 7.5, 300, "high_res.png")
}

}
