library(usethis)
library(gert)

# git_sitrep()

# git_status()

git_add(".")
git_commit_all("fejlfinding - overlay 'Ingen graf endnu Indtast data i tabellen til venstre eller upload en fil'")
git_push()
